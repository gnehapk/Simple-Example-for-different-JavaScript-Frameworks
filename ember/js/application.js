window.Todos = Ember.Application.create();   //create a new instance of Ember.Application 

Todos.ApplicationAdapter = DS.FixtureAdapter.extend();  // Adapters are responsible for communicating with a source of data for your application.
